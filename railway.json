{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan optimize && (if [ -z \"$APP_KEY\" ]; then export APP_KEY=$(php artisan key:generate --show); fi) && php artisan serve --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
